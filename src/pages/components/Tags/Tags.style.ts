import { createUseStyles } from "react-jss";

const useTagsStyle = createUseStyles({
  tag: {
    margin: 4,
    cursor: "pointer",
    fontSize: 18,
    background: "white",
  },
});

export default useTagsStyle;
