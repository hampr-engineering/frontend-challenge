describe('hooks/useFilteredCharacter', () => {
    describe('default', () => {
        test.todo('correct initial filterState');
        test.todo('correct initial filteredCharacters');
    });

    describe('handleNameOnChange', () => {
        test.todo('updates filterState.name');
    });

    describe('filteredCharacters', () => {
        test.todo('correct array is returned with different filterState.name=%p');
    });
});