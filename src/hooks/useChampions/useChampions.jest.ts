describe('hooks/useChampions', () => {
    describe('updateChampion', () => {
        test.todo('removes champion from state if already present');
        test.todo('adds champion to state if not present');
        test.todo('allows a maximum of 6 champions');
    });
});